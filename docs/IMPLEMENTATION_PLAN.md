# Implementation Plan — ROMAN AI (MVP)

Приоритеты: **P0** — без этого MVP не работает. **P1** — нужно для надёжной автономной работы. **P2** — расширение охвата площадок/качества. **P3** — можно отложить надолго. Решения по каждому пункту — см. `docs/adr/`.

---

## P0 — без этого MVP не работает

### ✅ DONE — репозиторий и командный центр
- Создан GitHub-репозиторий `github.com/Tima77777777/roman-ai`, git настроен локально.
- `README.md`, `.gitignore` (исключает `.env`), `.env.example` — на месте.
- `docs/TZ_AI_Company_Hybrid.md`, `docs/TECHNOLOGY_AUDIT.md`, `docs/ARCHITECTURE.md`, `docs/adr/*` — в репозитории.

### ✅ DONE — Telegram-интерфейс
- Bot создан и проверен (`@MyClaudhelp55555_bot`), токен в `.env`.
- `TELEGRAM_OWNER_ID` определён и сохранён.
- Owner-only фильтрация реализована в промпте исполнителя.

### ✅ DONE — исполнитель (временный)
- `state/telegram_offset.json` — механизм offset для `getUpdates`.
- Локальный `CronCreate`-поллер каждые 7 минут — задача создана, работает.
- **Известное ограничение:** живёт только пока сессия открыта, истекает через 7 дней (ADR-003).

### ✅ DONE — подключить GitHub App к claude.ai Routines
- Решено 2026-09-01: кнопка нашлась не в Settings, а на самой странице **claude.ai/code → New** — чеклист "Two steps to work in your repo" → **"Install on GitHub"**. Установлено с ограничением на репозитории `roman-ai` + `fin-tracker-bot` (не на все, из соображений минимальных прав). Подробности и полная история диагностики — `docs/ARCHITECTURE.md` раздел 3.2.

### ✅ DONE — создать Cloud Routine (репозиторий пока НЕ возвращён в private)
- Routine "Telegram poller" создана и **проверена реальным прогоном** (не только созданием): агент сам прочитал контекст (`ARCHITECTURE.md`, `policy.json`), обратился к Telegram `getUpdates` с токеном из окружения, получил пустой список и корректно ничего не сделал (правильное поведение при отсутствии новых сообщений). cron `33 * * * *` — раз в час, следующий прогон автоматический, без участия владельца.
- **По пути поймали реальную ошибку** (человеческий фактор, не архитектурная): в переменные окружения сначала попал токен от ДРУГОГО бота (fin-tracker-bot) вместо ROMAN AI — вскрылось через `409 Conflict` от Telegram, агент сам расследовал причину вместо слепого падения. Исправлено, перепроверено.
- **Технические детали, которых не было в плане заранее**: секретов для облачного окружения (`environment_id`) через API создать нельзя, только через `claude.ai/code → New → repo → Cloud → Add cloud environment`; поле "Environment variables" там прямо помечено как небезопасное для секретов (нет отдельного secrets-хранилища в текущем UI) — для одиночного личного аккаунта это приемлемый компромисс (токен не в git, виден только самому владельцу), но не то же самое, что настоящий secrets-менеджер.
- ⬜ TODO — вернуть репозиторий в private (сейчас всё ещё публичный от временного обхода, актуальность обхода отпала).
- ⬜ TODO — локальный `CronCreate`-поллер можно удалить как избыточный (Cloud Routine его функционально заменяет), но явно не удалялся — можно оставить как резерв, ничего не сломает.

### Instagram — App Review НЕ требуется для публикации в собственный аккаунт
- **Важное уточнение по ходу настройки**: изначально план предполагал 2–6-недельное ожидание модерации Meta App Review. По факту это не так, если приложение публикует только в СВОИ ЖЕ Instagram-аккаунты (не в чужие, не SaaS для посторонних пользователей) — в этом случае достаточно роли "Instagram-тестировщик" на аккаунте, без полной проверки приложения и без верификации бизнеса. Обнаружено и подтверждено практически при живой настройке.
- ✅ DONE (владельцем) — Meta-приложение создано ("ROMAN Content Bot", App ID `1041464175184361`), сценарий "Управление сообщениями и контентом в Instagram" настроен, необходимые разрешения добавлены (`instagram_business_basic`, `instagram_business_manage_comments`, `instagram_business_manage_messages`), Instagram-аккаунт `roman.demidov.official` (Business Account ID `17841403111143576`) добавлен как тестировщик и приглашение принято.
- ⬜ TODO — сгенерировать access-токен (уперлись в HTTP 429 от Instagram при генерации — временный rate-limit, ждём и повторяем) и App Secret, положить в `.env` как `INSTAGRAM_ACCESS_TOKEN` / `INSTAGRAM_APP_SECRET`. **Требует владельца** (кнопка "Сгенерировать маркер" в кабинете Meta).
- ⬜ TODO (только если решим публиковать и туда) — TikTok Developer-аккаунт, заявка на публикацию через API — отдельная площадка, свои сроки модерации, ещё не начато.

### базовый пайплайн ROMAN RAW (шаги 1–8 из ARCHITECTURE.md раздел 2)
- ✅ DONE — транскрипция: `scripts/transcribe.py` (self-hosted faster-whisper), протестировано на реальном голосовом сообщении владельца.
- ✅ DONE — монтаж/субтитры/9:16 экспорт: `scripts/edit_clip.py` (FFmpeg), протестировано end-to-end на синтетическом видео (нарезка сегментов, вжигание субтитров, вертикальный кроп для Shorts/Reels/TikTok).
- ⬜ TODO — выделение "лучших моментов" и хук/сценарий: это не отдельный скрипт, а рассуждение самого исполнителя (Claude) над транскриптом — реализуется как часть промпта при реальном прогоне, а не кодом.
- ✅ DONE — `docs/BRAND.md`: шаблон brandbook для ROMAN (идентичность/аудитория/голос/стоп-лист/editorial risk), подключён к шагам 4/5/8 пайплайна в ARCHITECTURE.md. Идея — из разбора внешнего источника (georgiirivera.ai), независимо подтвердила пробел, найденный ещё в первом ревью ТЗ. **Заполнение конкретики (примеры голоса, стоп-лист) — TODO для владельца**, шаблон сам по себе уже пригоден к использованию с общими значениями по умолчанию.
- ⬜ TODO — новый шаг 7.5 (подписи/CTA под конкретную площадку) добавлен в ARCHITECTURE.md, но кода/промпта для него ещё нет — реализовать вместе с шагом 4/5 (тоже рассуждение Claude, не отдельный скрипт).
- ✅ DONE (частично) — `scripts/download_from_drive.py` написан и проверен (парсинг ссылки/ID, resumable download): корректно падает без OAuth, аналогично YouTube/Facebook.
- ✅ DONE — Google Drive OAuth настроен и **проверен реальной авторизацией** — переиспользован тот же Google Cloud проект и `client_secret.json`, что и для YouTube (отдельный токен `scripts/.gdrive_token.json`, отдельный scope `drive.readonly`). Один клик "Продолжить" в браузере, `AUTH SUCCESS` подтверждён.
- ✅ DONE — `scripts/run_pipeline_dryrun.py`: сквозной прогон transcribe → edit_clip → mock-publish на видео с РЕАЛЬНОЙ речью (без реальных сетевых вызовов). Нашёл и исправил реальный баг: кириллица в транскрипте превращалась в кракозябры при передаче между Python-процессами на Windows (нужен явный `PYTHONIOENCODING=utf-8`).
- Tests: end-to-end прогон на одном тестовом видео (5–10 минут) от РЕАЛЬНОГО владельца — ещё предстоит (пока только синтетическое видео + реальная речь из голосового сообщения).
- Acceptance criteria: MVP-цикл из раздела 8 ТЗ выполняется вручную (owner triggers) хотя бы один раз целиком, включая реальную публикацию.

### публикация на YouTube, Facebook и Instagram
- ✅ DONE (частично) — `scripts/publish_youtube.py` написан и проверен: корректно и понятно падает, если OAuth не настроен (не крашится непонятно).
- ✅ DONE — OAuth настроен и **проверен реальной авторизацией**: Google Cloud проект `roman-ai-507216`, YouTube Data API v3 включён, OAuth-клиент (Desktop app) создан, `client_secret.json` лежит в `secrets/youtube_client_secret.json` (путь в `.env` как `YOUTUBE_CLIENT_SECRETS_FILE`, сам файл и `scripts/.youtube_token.json` — в `.gitignore`, не в git). Владелец добавлен как test user (приложение пока не прошло полную проверку Google — для личного использования достаточно). `flow.run_local_server()` подтверждён вживую: браузер открылся автоматически, владелец нажал "Продолжить", токен получен и закэширован. **Была путаница с двумя одноимёнными Google Cloud проектами** (507215 и 507216, второй создан по ходу дела) — доведено до конца во втором, файлы синхронизированы.
- ⬜ TODO — реальная публикация видео ещё не проверена (только сама авторизация) — нужен готовый ролик из пайплайна ROMAN RAW, будет протестировано на первом реальном прогоне.
- ✅ DONE (частично) — `scripts/publish_facebook.py` написан и проверен (resumable upload flow, Reels): корректно падает без Page Access Token, аналогично YouTube.
- ⬜ TODO — сам Facebook Page + Meta-приложение + Page Access Token. **Это может сделать только владелец.**
- ✅ DONE (частично) — `scripts/publish_instagram.py` написан и проверен (Content Publishing API: media container → poll status → publish): корректно падает без токена. **Важное архитектурное отличие от Facebook** — Instagram API не принимает сырые байты видео, только `video_url`, который Instagram сам скачивает по HTTPS — значит перед публикацией видео должно быть где-то опубликовано с публичным URL (например, через Cloudflare-мост), локальный файл напрямую не прокатит.
- ⬜ TODO — Meta-приложение для Instagram почти готово (см. раздел выше про App Review), осталось получить `INSTAGRAM_ACCESS_TOKEN`/`INSTAGRAM_APP_SECRET` — уперлись в временный rate-limit (429) от Instagram при генерации токена, ждём и повторяем.
- Tests: тестовая публикация одного готового ролика на все три канала — после того как владелец пройдёт OAuth-шаги выше.
- Acceptance criteria: ролик опубликован, ссылка возвращена в отчёте Роману.

---

## P1 — нужно для надёжной автономной работы

- ✅ DONE — Cloudflare Worker код (ADR-003): `apps/telegram-bridge/` — написан, типизирован, `npx tsc --noEmit` проходит чисто. Owner-only allowlist и проверка webhook secret реализованы. **Задеплоено и проверено вживую** (2026-09-01): аккаунт Cloudflare к этому моменту уже существовал (тот же, что для fin-tracker-bot), так что блокер "нужен аккаунт владельца" снят сам собой. `https://roman-ai-telegram-bridge.tima-apps.workers.dev` — три синтетических запроса на реальный задеплоенный воркер подтвердили: неверный секрет → `403`, сообщение не от владельца → тихий `200` без побочных эффектов, сообщение от владельца с верным секретом → `200` **и реально создан GitHub Issue** (`#1`, прочитан напрямую через GitHub API, тестовый issue закрыт после проверки). **Вебхук реального Telegram-бота на этот мост НЕ переключён намеренно** — `.github/workflows/telegram-issue-trigger.yml` (мост Issue → Routine `/fire`) ещё не написан и зависит от пункта выше (подключение GitHub App к Routines); переключение сейчас означало бы, что реальные сообщения Романа создавали бы issues, которые никто не обрабатывает — тихая деградация вместо улучшения. Переключать вебхук только после того, как цепочка Issue → Routine подтверждена целиком.
- ⬜ TODO — `.github/workflows/telegram-issue-trigger.yml` (issue opened → Routine `/fire`) — ещё не написан, зависит от подключения GitHub App к Routines (см. P0 выше).
- ✅ DONE — явный whitelist автономных действий: `policy.json` + `scripts/policy_check.py`, формализует правило из ARCHITECTURE.md 1.3 как проверяемый список (`always_allowed` / `requires_owner_approval`, дефолт — approval), протестирован.
- ✅ DONE — idempotency для публикации: `scripts/publish_guard.py` (хеш файла + платформа → `state/published.json`), подключён к `publish_youtube.py`, `publish_facebook.py` и `publish_instagram.py`, протестирован.
- ✅ DONE — failure handling: retry с backoff (3 попытки, 5 сек) в `scripts/transcribe.py` и `scripts/edit_clip.py`, регрессия пройдена после изменения.
- ✅ DONE — реальный лимит `getFile` проверен эмпирически: **ровно 20 МБ** (загружал/скачивал файлы 15/19/20/21/25/30 МБ через реальный Bot API — 20 успех, 21 отказ), см. `docs/TECHNOLOGY_AUDIT.md`. Тестовые файлы отправлялись и сразу удалялись из чата владельца.
- **Остальные UNKNOWN из Technology Audit**: гео-ограничения ElevenLabs (если/когда голосовое клонирование понадобится), поддержка Reels в Buffer (если решат туда переходить) — не проверены, не блокируют текущий MVP (ElevenLabs/Buffer не выбраны).
- ✅ DONE — обработка крайних случаев: проверка наличия ffmpeg (`edit_clip.py`), проверка существования входных файлов до начала работы (все скрипты, включая до старта OAuth-флоу в publish_*), валидация сегментов (start/end корректны), явное предупреждение при пустой транскрипции. Регрессия (`run_pipeline_dryrun.py`) и новые сценарии ошибок проверены вручную — всё работает.
- ✅ DONE (написано и лежит локально, **не в git** — токен без scope `workflow`, GitHub блокирует любой push, где такой коммит есть в истории) — базовый CI: `.github/workflows/ci.yml` (компиляция всех Python-скриптов + валидация JSON-состояния при каждом push). Файл на диске, закоммичу и запушу отдельно, как только придёт токен с нужным scope.
- ⬜ TODO — токен с scope `workflow` (плюс `repo`). **Объединено с задачей ротации токена.**
- ✅ DONE — юнит-тесты чистой логики: `scripts/test_pure_logic.py` (extract_file_id, policy_check, publish_guard round-trip на изолированном временном state-файле, не трогает реальный `state/published.json`), все проходят.

## P2 — расширение охвата и качества

- **X (Twitter) API** — подключить, когда объём контента оправдает обязательную привязку карты и pay-per-use.
- **n8n self-hosted** — развернуть на VPS для оркестрации публикации между площадками вместо прямых вызовов из пайплайна.
- **Улучшение качества авто-монтажа** — пересмотреть в сторону Shotstack или платных планов Opus Clip/Submagic, если самодельный FFmpeg-пайплайн (ADR-005) не даёт приемлемого качества после практической проверки.

## P3 — можно отложить надолго

- **AI Digital Avatar и voice cloning** (HeyGen/ElevenLabs) — вне MVP по решению владельца, ROMAN RAW работает с реальным видео.
- **MODE 1 (TREND) и MODE 3 (IDEA)** пайплайны из полного ТЗ.
- **S3/R2 хранилище** взамен Google Drive — только если квоты Google Drive реально станут узким местом.
- **Расширение на остальные 8 брендов и департаменты** (Research, Marketing, CRM/Sales, Finance/Crypto) — по решению владельца, после того как этот MVP заработает стабильно на одном бренде.
- **Юридические/комплаенс/GDPR/CRM-вопросы** из полного ТЗ — осознанно отложены владельцем до появления сайта/CRM (см. память проекта `project_roman_scope`), не блокируют текущий P0–P2.
